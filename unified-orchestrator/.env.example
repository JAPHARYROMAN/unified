# ─── App ───
NODE_ENV=development
PORT=3000

# ─── Database ───
DATABASE_URL=postgresql://unified:unified_secret@localhost:5432/unified_orchestrator?schema=public

# ─── Admin auth (REQUIRED — minimum 16 chars, protects /admin/* routes) ───
# Generate with: openssl rand -hex 32
ADMIN_API_KEY=

# ─── CORS (REQUIRED — comma-separated list of allowed browser origins) ───
# Example: https://app.unified.finance,https://admin.unified.finance
# Use http://localhost:3001 for local frontend dev
CORS_ORIGINS=http://localhost:3001
